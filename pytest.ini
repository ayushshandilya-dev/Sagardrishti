[pytest]
# Ensure the repo root is importable so tests can `import core/api/data`
# regardless of how pytest is invoked (bare `pytest tests/` in CI does NOT
# add the CWD to sys.path; `python -m pytest` happens to).
pythonpath = .